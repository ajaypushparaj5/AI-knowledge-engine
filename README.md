# AI Knowledge Engine

A local semantic search and document retrieval system built using embeddings and cosine similarity.

## Features
- PDF ingestion
- Chunking with overlap
- Persistent vector storage
- Semantic retrieval
- Threshold filtering

## How to run

1. Create venv
2. Install requirements
3. Run main.py
